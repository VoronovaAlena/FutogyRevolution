<Window x:Class="TrafficUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TrafficUI"
        mc:Ignorable="d"
        WindowState="Normal"
        Title="Traffic UI"
        
        d:DesignWidth="1920"
        d:DesignHeight="1080">
    <Window.Resources>
        <Style TargetType="{x:Type GroupBox}">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="Margin" Value="24,13,24,3" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GroupBox}">
                        <Grid HorizontalAlignment="Stretch"
                          VerticalAlignment="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"
                                              SharedSizeGroup="HeaderColumn" />
                                <ColumnDefinition Width="2" />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>

                            <ContentPresenter Grid.Column="0"
                                          ContentSource="Header">
                                <ContentPresenter.Resources>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="FontSize" Value="16" />
                                        <Setter Property="TextWrapping" Value="Wrap" />
                                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Foreground" Value="Gray" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>

                            <ContentPresenter Grid.Column="2"
                                          HorizontalAlignment="Stretch"
                                          ContentSource="Content" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="TextElement.Foreground" Value="Gray" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Grid.Column="0" 
                Grid.RowSpan="2" 
                Margin="20 20 0 0"
                Height="60"
                Width="200"
                Content="Start" 
                Click="Button_Click"
                HorizontalAlignment="Left" 
                VerticalAlignment="Top" FontSize="16" />

        <GroupBox Grid.Row="0" Grid.Column="1" Header="Controller1:" Name="Controller1">
            <TextBlock Text="{Binding Path=Controller1, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox Grid.Row="1" Grid.Column="1" Header="Controller2:" Name="Controller2">
            <TextBlock Text="{Binding Path=Controller2, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="Controller3" Grid.Row="2" Grid.Column="1" Header="Controller3:" >
            <TextBlock Text="{Binding Path=Controller3, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="Controller4" Grid.Row="3" Grid.Column="1" Header="Controller4:" >
            <TextBlock Text="{Binding Path=Controller4, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="Controller5" Grid.Row="4" Grid.Column="1" Header="Controller5:" >
            <TextBlock Text="{Binding Path=Controller5, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="Controller6" Grid.Row="5" Grid.Column="1" Header="Controller6:" >
            <TextBlock Text="{Binding Path=Controller6, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="Controller7" Grid.Row="6" Grid.Column="1" Header="Controller7:" >
            <TextBlock Text="{Binding Path=Controller7, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="Controller8" Grid.Row="7" Grid.Column="1" Header="Controller8:" >
            <TextBlock Text="{Binding Path=Controller8, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="Controller9" Grid.Row="8" Grid.Column="1" Header="Controller9:" >
            <TextBlock Text="{Binding Path=Controller9, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="Controller10" Grid.Row="9" Grid.Column="1" Header="Controller10:" >
            <TextBlock Text="{Binding Path=Controller10, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>
        
        

        <GroupBox x:Name="p1" Grid.Row="0" Grid.Column="2" Header="Phase:" >
            <TextBlock Text="{Binding Path=Phase1, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="p2" Grid.Row="1" Grid.Column="2" Header="Phase:" >
            <TextBlock Text="{Binding Path=Phase2, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="p3" Grid.Row="2" Grid.Column="2" Header="Phase:" >
            <TextBlock Text="{Binding Path=Phase3, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="p4" Grid.Row="3" Grid.Column="2" Header="Phase:" >
            <TextBlock Text="{Binding Path=Phase4, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="p5" Grid.Row="4" Grid.Column="2" Header="Phase:" >
            <TextBlock Text="{Binding Path=Phase5, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="p6" Grid.Row="5" Grid.Column="2" Header="Phase:" >
            <TextBlock Text="{Binding Path=Phase6, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="p7" Grid.Row="6" Grid.Column="2" Header="Phase:" >
            <TextBlock Text="{Binding Path=Phase7, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="p8" Grid.Row="7" Grid.Column="2" Header="Phase:" >
            <TextBlock Text="{Binding Path=Phase8, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="p9" Grid.Row="8" Grid.Column="2" Header="Phase:" >
            <TextBlock Text="{Binding Path=Phase9, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

        <GroupBox x:Name="p10" Grid.Row="9" Grid.Column="2" Header="Phase:" >
            <TextBlock Text="{Binding Path=Phase10, UpdateSourceTrigger=PropertyChanged}" FontSize="16" />
        </GroupBox>

    </Grid>
</Window>
